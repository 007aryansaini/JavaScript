'use strict';

const arr = [10, 20, 30, 40, 50, 60];

const convt = 10;

// using array function
const newArr = arr.map(curr => convt * curr);
console.log(arr);
console.log(newArr);


---> The map function is just like forEach method it is also used for looping but it returns the new array of the values returned in each iterartion.
